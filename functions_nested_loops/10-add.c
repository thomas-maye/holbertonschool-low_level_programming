#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: a is an int will be add to b
 * @b: b is an int will be add to a
 * Return: sum of a and b
 */

int add(int a, int b)
{
	return (a + b);
}
