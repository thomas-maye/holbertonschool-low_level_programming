#include "main.h"
#include <stdio.h>

/**
 * _strlen - a function to returns the length of a string
 * @s: my string
 * Return: Always 0
 */

int _strlen(char *s)
{
	int lenght;

	for (lenght = 0; s[lenght] != 0; lenght++)
	{
	}
	return (lenght);
}
